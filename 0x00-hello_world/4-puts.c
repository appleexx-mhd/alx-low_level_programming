#include<stdio.h>
/**
 * main - a program that pritns a line with puts function
 * Return: Always 0 (Success)
 */

int main(void)
{
	char C[] = "\"Programming is like building a multilingual puzzle";

	puts(C);

	return (0);
}
