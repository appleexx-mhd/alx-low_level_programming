#include "main.h"
/**
 * _isalpha - cheks for alphabetic caracters
 * Return: 1 if c is a letter ,0 otherwise
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z')||(c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
}
